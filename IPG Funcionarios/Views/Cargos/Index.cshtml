@model IEnumerable<IPG_Funcionarios.Models.Cargo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCargo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCargo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CargoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CargoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CargoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
